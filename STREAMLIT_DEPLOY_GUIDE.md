# 📸 Streamlit Cloud 배포 완벽 가이드 (클릭 따라하기)

## 🎯 목표: claude 브랜치를 Streamlit Cloud에 배포 (5분 완성)

---

## Step 1: Streamlit Cloud 로그인

### 🌐 웹사이트 열기:
```
https://share.streamlit.io
```

### 🖱️ 할 일:
1. 위 링크를 **복사해서 새 탭**에 붙여넣기
2. **엔터** 키 누르기

### ✅ 로그인 화면:
```
┌─────────────────────────────────────────────────────┐
│                                                     │
│              Streamlit Community Cloud              │
│                                                     │
│          [Continue with GitHub] 버튼                │
│                                                     │
│          (또는 이미 로그인되어 있음)                  │
│                                                     │
└─────────────────────────────────────────────────────┘
```

### 🖱️ 할 일:
- **"Continue with GitHub"** 버튼 클릭
- (이미 로그인되어 있으면 대시보드가 보임)

---

## Step 2: 새 앱 만들기

### ✅ 대시보드 화면:
```
┌─────────────────────────────────────────────────────┐
│  Streamlit Community Cloud           [프로필 아이콘] │
├─────────────────────────────────────────────────────┤
│                                                     │
│  Your apps                              [New app ▼]│
│                                                     │
│  (앱 목록 또는 "No apps yet" 메시지)                  │
│                                                     │
└─────────────────────────────────────────────────────┘
```

### 🖱️ 할 일:
1. 우측 상단의 **"New app"** 버튼 클릭
2. 드롭다운에서 **"From existing repo"** 선택

### ✅ 다음 화면:
```
┌─────────────────────────────────────────────────────┐
│  Deploy an app                                      │
├─────────────────────────────────────────────────────┤
│                                                     │
│  Repository *                                       │
│  ┌─────────────────────────────────────────────┐   │
│  │ [레포지토리 선택 드롭다운]                     │   │
│  └─────────────────────────────────────────────┘   │
│                                                     │
│  Branch *                                           │
│  ┌─────────────────────────────────────────────┐   │
│  │ main                                        │   │
│  └─────────────────────────────────────────────┘   │
│                                                     │
│  Main file path *                                   │
│  ┌─────────────────────────────────────────────┐   │
│  │ streamlit_app.py                            │   │
│  └─────────────────────────────────────────────┘   │
│                                                     │
│  App URL (optional)                                 │
│  ┌─────────────────────────────────────────────┐   │
│  │                                             │   │
│  └─────────────────────────────────────────────┘   │
│                                                     │
│                            [Deploy!] 버튼           │
│                                                     │
└─────────────────────────────────────────────────────┘
```

---

## Step 3: Repository 선택

### 🖱️ 할 일:
1. **"Repository"** 드롭다운 클릭
2. 목록에서 **"mhroh/idebate"** 찾아서 클릭

### ⚠️ 만약 목록에 없으면:
1. 페이지 하단의 "Can't find your repo?" 링크 클릭
2. GitHub permissions 설정 → idebate 레포지토리 접근 허용
3. 돌아와서 새로고침

---

## Step 4: Branch 변경 ⭐ (가장 중요!)

### 🖱️ 할 일:
1. **"Branch"** 드롭다운 클릭

### ✅ 드롭다운 열림:
```
┌─────────────────────────────────────┐
│ ○ main                              │
│ ○ claude/analyze-unknown-task-...  │ ← 이것!
│ ○ claude/organize-conversation-... │
└─────────────────────────────────────┘
```

### 📋 선택할 브랜치:
```
claude/analyze-unknown-task-011CUqkenKT94Az5cryNitAm
```

### 🖱️ 할 일:
- **"claude/analyze-unknown..."** 로 시작하는 브랜치 클릭

### ⚠️ 주의:
- **main이 아닙니다!** ❌
- **claude/analyze-unknown...** 이어야 합니다! ✅

---

## Step 5: Main file path 변경

### 현재 값:
```
streamlit_app.py
```

### 📋 변경할 값:
```
app.py
```

### 🖱️ 할 일:
1. **"Main file path"** 칸 클릭
2. 기존 내용 **전체 삭제** (Ctrl+A 후 Delete)
3. `app.py` 입력

---

## Step 6: App URL 설정 (선택)

### 📋 권장 URL:
```
idebate-optimized
```

### 🖱️ 할 일:
1. **"App URL"** 칸 클릭
2. `idebate-optimized` 입력

### 💡 참고:
- 비워두면 자동 생성됨
- 나중에 변경 가능

---

## Step 7: 최종 확인

### ✅ 입력값 확인:
```
Repository:    mhroh/idebate
Branch:        claude/analyze-unknown-task-011CUqkenKT94Az5cryNitAm ⭐
Main file:     app.py
App URL:       idebate-optimized (선택)
```

### ⚠️ 체크리스트:
- [ ] Repository가 `mhroh/idebate`인가?
- [ ] Branch가 `claude/analyze-unknown...`인가? (main 아님!)
- [ ] Main file이 `app.py`인가? (streamlit_app.py 아님!)

---

## Step 8: 배포 시작!

### 🖱️ 할 일:
- 우측 하단의 **"Deploy!"** 버튼 클릭

### ✅ 배포 화면:
```
┌─────────────────────────────────────────────────────┐
│  idebate-optimized                 [⚙️ Manage app] │
├─────────────────────────────────────────────────────┤
│                                                     │
│  🔨 Building...                                     │
│                                                     │
│  ┌───────────────────────────────────────────────┐ │
│  │ Cloning repository...                         │ │
│  │ Installing Python 3.11...                     │ │
│  │ Installing dependencies...                    │ │
│  │ ...                                           │ │
│  └───────────────────────────────────────────────┘ │
│                                                     │
└─────────────────────────────────────────────────────┘
```

### ⏰ 대기 시간:
- **약 2-3분** (커피 한 잔 ☕)

---

## Step 9: Secrets 설정 (중요!)

### ⚠️ 타이밍:
배포가 진행되는 동안 또는 완료 후 즉시 설정해야 합니다!

### 🖱️ 할 일:
1. 배포 화면 우측 상단 **"⚙️ Manage app"** 클릭
2. 왼쪽 메뉴에서 **"Secrets"** 클릭

### ✅ Secrets 화면:
```
┌─────────────────────────────────────────────────────┐
│  Secrets                                            │
├─────────────────────────────────────────────────────┤
│                                                     │
│  Edit your secrets                                  │
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │                                             │   │
│  │                                             │   │
│  │  [여기에 Secrets 붙여넣기]                    │   │
│  │                                             │   │
│  │                                             │   │
│  └─────────────────────────────────────────────┘   │
│                                                     │
│                                   [Save] 버튼       │
│                                                     │
└─────────────────────────────────────────────────────┘
```

---

## Step 10: Secrets 값 입력

### 📋 복사할 내용 (⚠️ 실제 값으로 수정 필요!):

```toml
# Anthropic API Key
ANTHROPIC_API_KEY = "sk-ant-api03-여기에_실제_API_키_입력하세요"

# Google Sheets API (Service Account JSON)
type = "service_account"
project_id = "여기에_프로젝트_ID"
private_key_id = "여기에_키_ID"
private_key = "-----BEGIN PRIVATE KEY-----\n여기에_실제_Private_Key_전체\n-----END PRIVATE KEY-----\n"
client_email = "여기에_서비스계정_이메일@xxx.iam.gserviceaccount.com"
client_id = "여기에_클라이언트_ID"
auth_uri = "https://accounts.google.com/o/oauth2/auth"
token_uri = "https://oauth2.googleapis.com/token"
auth_provider_x509_cert_url = "https://www.googleapis.com/oauth2/v1/certs"
client_x509_cert_url = "여기에_인증서_URL"
universe_domain = "googleapis.com"

# Google Sheets Document URL
sheet_url = "https://docs.google.com/spreadsheets/d/여기에_시트_ID/edit"
```

### 🖱️ 할 일:
1. 위 내용을 **복사**
2. **"여기에_XXX"** 부분을 **실제 값으로 수정**
3. Secrets 칸에 **붙여넣기**
4. **"Save"** 버튼 클릭

### ⚠️ 중요:
- `private_key`는 `\n`을 포함한 전체 키를 입력해야 합니다
- 따옴표와 줄바꿈 형식을 정확히 유지하세요

---

## Step 11: 앱 재시작

### 🖱️ 할 일:
1. Secrets 저장 후 왼쪽 메뉴 **"Overview"** 클릭
2. 우측 상단 **"⋮"** (점 3개) 메뉴 클릭
3. **"Reboot app"** 선택

### ✅ 재시작 화면:
```
┌─────────────────────────────────────────────────────┐
│  🔄 Rebooting...                                    │
│                                                     │
│  Your app is restarting with the new secrets       │
│                                                     │
└─────────────────────────────────────────────────────┘
```

---

## Step 12: 배포 완료 확인

### ✅ 성공 화면:
```
┌─────────────────────────────────────────────────────┐
│  idebate-optimized                 [⚙️ Manage app] │
├─────────────────────────────────────────────────────┤
│                                                     │
│  ✅ Your app is live!                               │
│                                                     │
│  🌐 https://idebate-optimized.streamlit.app        │
│                                                     │
│  ┌───────────────────────────────────────────────┐ │
│  │                                               │ │
│  │         교육용 챗봇                            │ │
│  │                                               │ │
│  │    대화명을 입력하세요: [_______]             │ │
│  │                                               │ │
│  └───────────────────────────────────────────────┘ │
│                                                     │
└─────────────────────────────────────────────────────┘
```

### 🎉 성공!

---

## Step 13: 최초 테스트

### 🖱️ 할 일:
1. **앱 URL 복사**: `https://idebate-optimized.streamlit.app`
2. **새 탭**에서 열기
3. 테스트 진행:

#### ✅ 테스트 체크리스트:
```
[ ] 페이지가 로딩됨
[ ] "교육용 챗봇" 제목 보임
[ ] 대화명 입력 가능
[ ] 대화 시작 버튼 작동
[ ] 메시지 전송 가능
[ ] AI 응답 받음 (10초 내)
[ ] "평가 받기" 버튼 보임
[ ] 에러 메시지 없음
```

### ✅ 모두 통과하면:
**배포 성공!** 🎉

---

## 🆘 문제 해결

### ❌ "An error occurred"
**원인**: Secrets 설정 오류
**해결**:
1. Manage app → Secrets
2. API 키 확인
3. private_key 형식 확인
4. Save → Reboot app

### ❌ "Module not found"
**원인**: Branch 설정 오류
**해결**:
1. Manage app → Settings
2. Branch가 `claude/analyze-unknown...`인지 확인
3. `main`으로 되어 있으면 변경
4. Reboot app

### ❌ "File not found: streamlit_app.py"
**원인**: Main file path 오류
**해결**:
1. Manage app → Settings
2. Main file path를 `app.py`로 변경
3. Save → Reboot app

---

## ✅ 배포 완료!

이제 다음 단계로:
→ **MONITORING_CHECKLIST.md** 파일을 열어서 모니터링을 시작하세요!

---

## 📌 배포 정보 기록

배포 완료 후 아래 정보를 기록하세요:

```
배포 일시: _______________
앱 URL: https://idebate-optimized.streamlit.app
Branch: claude/analyze-unknown-task-011CUqkenKT94Az5cryNitAm
상태: ✅ 성공 / ❌ 실패

첫 테스트 결과:
- 로딩: ✅/❌
- 대화: ✅/❌
- AI 응답: ✅/❌
- 문제점: _______________
```

---

## 🎯 다음 단계

1. ✅ Streamlit Cloud 배포 완료
2. ⏳ 24-48시간 모니터링 (MONITORING_CHECKLIST.md)
3. ✅ 안정성 확인 후 PR 승인
4. 🎉 완료!
